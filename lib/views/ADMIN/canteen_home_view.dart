import 'package:flutter/material.dart';

class CanteenHomeView extends StatelessWidget {
  const CanteenHomeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Canteen Admin View"),
    );
  }
}
